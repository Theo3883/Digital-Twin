namespace DigitalTwin.Mobile.NativeHost;

internal static class Program
{
    // A real managed entry point is required so NativeAOT generates the correct
    // runtime startup plumbing (thread store, module registration, etc).
    public static int Main(string[] args) => 0;
}

